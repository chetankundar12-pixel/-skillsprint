<%
  // SkillSprint brand mark — a single monoline "S" stroke, not a
  // letterform in a font, so it reads as a crafted mark rather than a
  // placeholder initial. Uses currentColor so it always matches
  // .brand-mark's existing color:#fff.
  //
  // Drop it inside the existing badge div in place of the plain "S" text.
  // Reminder: never write EJS delimiter characters inside a comment —
  // see the note in partials/head.ejs.
%>
<svg width="24" height="24" viewBox="0 0 32 32" fill="none" xmlns="http://www.w3.org/2000/svg">
  <path
    d="M21.5 10.2c0-2.6-2.6-4.7-7-4.7s-7.5 2.3-7.5 5.4 3.4 4.6 7.5 5.1 7.5 2.1 7.5 5.3-3.3 5.4-7.5 5.4-6.5-1.9-6.7-4.4"
    stroke="currentColor"
    stroke-width="3.4"
    stroke-linecap="round"
    stroke-linejoin="round"
  />
</svg>
