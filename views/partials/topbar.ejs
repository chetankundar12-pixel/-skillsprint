<div class="topbar2">
  <button class="mobile-nav-toggle" type="button" aria-expanded="false" aria-controls="appSidebar" aria-label="Open menu">
    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"><line x1="4" y1="7" x2="20" y2="7"/><line x1="4" y1="12" x2="20" y2="12"/><line x1="4" y1="17" x2="20" y2="17"/></svg>
  </button>
  <div class="topbar-search">
    🔍 <input type="text" placeholder="Search courses, topics, or resources...">
  </div>
  <div class="topbar-bell">🔔<span class="dot"></span></div>
  <div class="topbar-user2">
    <% if (user.fullName && user.fullName.split(' ')[0].toLowerCase() === 'chetan') { %>
      <img src="/img/chetan.jpg" alt="<%= user.fullName %>">
    <% } else if (user.fullName && ['aaditya','aditiya'].includes(user.fullName.split(' ')[0].toLowerCase())) { %>
      <img src="/img/aaditya.jpg" alt="<%= user.fullName %>">
    <% } else { %>
      <div class="avatar"><%= (user.fullName || 'U').charAt(0).toUpperCase() %></div>
    <% } %>
    <span class="tu-name"><%= user.fullName ? user.fullName.split(' ')[0] : '' %></span> ▾
  </div>
</div>
